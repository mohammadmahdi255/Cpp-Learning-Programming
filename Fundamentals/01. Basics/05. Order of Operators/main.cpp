#include <iostream>

int main() {
    // ()
    // * and /
    // + and -
    double x = 10;
    double y = 5;
    double z = (x + 10) / (3 * y);
    std::cout << z << std::endl;

    return EXIT_SUCCESS;
}
